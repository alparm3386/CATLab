@model IEnumerable<CAT.Models.Entities.Main.Job>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Order)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quote)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SourceDocumentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FinalDocumentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateProcessed)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Order!.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quote!.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SourceDocumentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FinalDocumentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateProcessed)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
