namespace CAT.Enums
{
    public enum OEMode
    {
        Admin,
        Linguist,
        Client
    }

    public enum DocumentType
    {
        unspecified = -1,
        original,
        translated
    }
}
